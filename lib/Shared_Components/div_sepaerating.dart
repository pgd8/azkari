import 'package:flutter/material.dart';

class DivSeperating extends StatelessWidget {
  const DivSeperating({super.key});

  @override
  Widget build(BuildContext context) {
    return const Divider(
      thickness: 2,
    );
  }
}
